// Wait for the document to be ready
$(document).ready(function() {
  // Code to execute when the document is ready

  // Example: Add event listeners, initialize components, etc.

  // You can start by adding your code here.
});
